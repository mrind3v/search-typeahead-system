# Demo Screenshots

Optional captures that illustrate the running system:

- Search UI with suggestions (3+ characters typed)
- Trending searches panel
- `curl http://localhost:8000/metrics` JSON output
- `curl "http://localhost:8000/cache/debug?prefix=iph"` routing inspection
