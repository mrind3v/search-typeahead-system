# Demo Screenshots

Add screenshots here for submission and viva:

- Search UI with suggestions (3+ characters typed)
- Trending searches panel
- `curl http://localhost:8000/metrics` JSON output
- `curl http://localhost:8000/debug/cache?q=iph` routing inspection
